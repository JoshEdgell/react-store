import React from 'react';
import { Button } from 'reactstrap';

function Create(props){
    return(
        <div id='create-products' className='product-divs'>
            <Button className='toggle'>Create Items</Button>
        </div>
    )
};

export default Create;