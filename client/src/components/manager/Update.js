import React from 'react';
import { Button } from 'reactstrap'

function Update(props){
    return(
        <div id='update-products' className='product-divs'>
            <Button className='toggle'>Update Items</Button>
        </div>
    )
};

export default Update;